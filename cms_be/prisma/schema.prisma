generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model demo {
  id        Int     @id @default(autoincrement())
  user_name String  @db.Char(1)
  message   String?
}
